export type Product = {
  id: string;
  name: string;
  category: string;
  price: number;
  discountPrice: number;
  rating: number;
  soldCount: number;
  description: string;
  image: string;
  isRecommended: boolean;
  aiReason: string;
  stock: number;
  badge: string;
};
